environment:
  name: dev

compute:
  gpus: 4
  distributed:
    strategy: ddp

mlflow:
  tracking_uri: http://mlflow.lab.com:5000
  experiment_name: gpt2-finetune

storage:
  checkpoint_dir: /shared/checkpoints/gpt2-finetune
  output_dir: /shared/outputs/gpt2-finetune

job:
  name: gpt2-finetune-run-01
  resume: auto
  max_retries: 1
