# ConvNeXt Base — 현대적 순수 합성곱 아키텍처
name: convnext-base
family: convnext
class_path: timm.create_model
head_builtin: false

pretrained_sources:
  - "timm://convnext_base.fb_in22k_ft_in1k"
  - "hf://facebook/convnext-base-224"

supported_tasks:
  - image_classification
  - feature_extraction

default_head:
  task: image_classification
  slot: head.cls
  _component_: ClassificationHead
  hidden_dim: 1024
  dropout: 0.1

input_spec:
  channels: 3
  height: 224
  width: 224
  dtype: float32

recipe_defaults:
  image_classification:
    model:
      pretrained: "timm://convnext_base.fb_in22k_ft_in1k"
      num_classes: "???"
    data:
      dataset:
        _component_: ImageClassificationDataset
        source: "???"
        split: train
        fields: {image: "???", label: "???"}
        augmentation:
          - type: RandomResizedCrop
            params: {size: 224}
          - type: RandomHorizontalFlip
          - type: ToImage
          - type: ToDtype
            params: {dtype: float32, scale: true}
          - type: Normalize
            params: {mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
      val_dataset:
        _component_: ImageClassificationDataset
        source: "???"
        split: validation
        fields: {image: "???", label: "???"}
        augmentation:
          - type: Resize
            params: {size: 256}
          - type: CenterCrop
            params: {size: 224}
          - type: ToImage
          - type: ToDtype
            params: {dtype: float32, scale: true}
          - type: Normalize
            params: {mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
      collator:
        _component_: VisionCollator
      dataloader:
        batch_size: 64
        num_workers: 8
    training:
      epochs: 20
      precision: bf16
      gradient_accumulation_steps: 1
      gradient_clip_max_norm: 1.0
    optimizer:
      _component_: AdamW
      lr: 5.0e-5
      weight_decay: 0.05
    scheduler:
      _component_: CosineAnnealingLR
      T_max: 20
      warmup_ratio: 0.05
    evaluation:
      metrics: [Accuracy, F1Score]
    metadata:
      author: "???"
      description: "???"
  feature_extraction:
    model:
      pretrained: "timm://convnext_base.fb_in22k_ft_in1k"
      num_classes: 0
    data:
      dataset:
        _component_: ImageClassificationDataset
        source: "???"
        split: train
        fields: {image: "???"}
        augmentation:
          - type: Resize
            params: {size: 256}
          - type: CenterCrop
            params: {size: 224}
          - type: ToImage
          - type: ToDtype
            params: {dtype: float32, scale: true}
          - type: Normalize
            params: {mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
      collator:
        _component_: VisionCollator
      dataloader:
        batch_size: 128
        num_workers: 8
    training:
      max_steps: 1
      precision: bf16
    metadata:
      author: "???"
      description: "???"

init_args_schema:
  num_classes:
    type: int
    default: 1000
  pretrained:
    type: bool
    default: true

adapter_defaults:
  lora:
    # timm ConvNeXt 블록: depthwise conv(`conv_dw`) + pointwise MLP(`fc1`, `fc2`)
    target: [conv.dw, mlp.fc1, mlp.fc2]
    r: 8
    alpha: 16

memory:
  params_m: 88.6
  fp32_gb: 0.34
  fp16_gb: 0.17
