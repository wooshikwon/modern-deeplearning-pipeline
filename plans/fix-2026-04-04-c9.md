# 수정 리포트 (Cycle 9)

## 처리 항목

| 리뷰 ID | 제목 | 결과 | 비고 |
|---------|------|------|------|
| 1-1 | GenerateHandler `acquire_nowait()` 크래시 | 수정 완료 | `asyncio.Lock` + `locked()` 패턴으로 교체 |
| 1-2 | SFT on_batch_start raw enumerate index | 수정 완료 | 대칭: RLTrainer는 이미 올바름 |
| 1-3 | `_validate_fallback` non-dict `output` 속성 미시도 | 수정 완료 | 대칭: `_compute_loss` dict 분기는 올바름 |
| 1-4 | 에폭 마지막 residual flush NaN 가드 부재 | 수정 완료 | 대칭: RLTrainer는 `backward_and_step` 사용으로 해당 없음 |
| 2-1 | adapter 검증 로직 중복 (~40줄) | 수정 완료 | `_validate_single_adapter()` 공통 함수 추출 |
| 2-2 | DeepSpeed `setup_models` optimizer 누락 | 수정 완료 | base/FSDP/DS 인터페이스 + RLTrainer caller 갱신 |
| 2-3 | `_get_or_create` None 캐싱 | 수정 완료 | None 반환 시 캐시 저장 건너뜀 |
| 2-4 | InferenceResult `run_id` 필드 누락 | 수정 완료 | Pydantic 모델에 필드 추가 |
| 2-5 | florence-2-base dead `default_head` | 수정 완료 | `head_builtin: true`이므로 섹션 제거 |
| 2-6 | Mixtral `lora_with_experts` 키 혼동 | 수정 완료 | `_lora_expert_preset`으로 rename + 주석 |
| 2-7 | ServingConfig `backend` dead field | [무효] | `CompatValidator._check_serving_backend`에서 참조 — dead field 아님 |
| 2-8 | `_select_collator` dead code | 수정 완��� | 내부 tokenizer 생성 → 방어적 `ValueError`로 교체 |
| 3-1 | spec: `check_rl_consistency` 위치 명시 | [변경없음] | spec-schema.md:98, spec-architecture.md:57에 이미 명시됨 |
| 3-2 | spec-data: 처리 순서 코드 불일치 | 수정 완료 | collator 생성을 4번(데이터 로딩 전)으로 이동 |

## 변경 목록

- `mdp/serving/handlers.py`: [1-1] `asyncio.Semaphore` → `asyncio.Lock` + `locked()` 패턴
- `mdp/training/trainer.py`: [1-2] `on_batch_start step=step` → `step=self.global_step`
- `mdp/training/trainer.py`: [1-3] `_validate_fallback` non-dict 분기에 `getattr(outputs, "output", None)` 추가
- `mdp/training/trainer.py`: [1-4] residual flush에 inf/NaN gradient 체크 추가, NaN 시 step/callback 건너뜀
- `mdp/settings/validation/business_validator.py`: [2-1] `_validate_single_adapter()` 추출, SFT/RL 양쪽 사용
- `mdp/training/strategies/base.py`: [2-2] `setup_models`에 `optimizers` 파라미터 추가
- `mdp/training/strategies/deepspeed.py`: [2-2] `setup_models`에서 optimizer를 `deepspeed.initialize`에 전달
- `mdp/training/strategies/fsdp.py`: [2-2] 인터페이스 시그니처 맞춤
- `mdp/training/rl_trainer.py`: [2-2] `setup_models` 호출 시 `optimizers=self.optimizers` 전달
- `mdp/factory/factory.py`: [2-3] `_get_or_create`에서 None 반환 시 캐시 저장 건너뜀
- `mdp/cli/schemas.py`: [2-4] `InferenceResult`에 `run_id: str | None = None` 추가
- `mdp/models/catalog/text_generation/florence-2-base.yaml`: [2-5] dead `default_head` 섹션 제거
- `mdp/models/catalog/text_generation/mixtral-8x7b.yaml`: [2-6] `lora_with_experts` → `_lora_expert_preset` 키 rename
- `mdp/data/dataloader.py`: [2-8] `_select_collator` 내부 tokenizer 생성 코드 → `ValueError` 방어

## 문서 동기화

- `docs/spec-data.md`: [3-2] 처리 순서를 실제 코드와 일치 — collator 생성을 데이터 로딩 전(4번)으로 이동

## 추가 발견

없음.
