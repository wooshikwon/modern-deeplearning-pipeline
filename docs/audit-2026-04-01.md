# MDP (Modern Deeplearning Pipeline) 설계-구현 정합성 감사

> **감사일**: 2026-04-01
> **최종 갱신**: 2026-04-01 (JobManager 제거, MLflow 로깅 개선, val_check 재설계, warmup 검증 반영)
> **검증 상태**: sub-agent 1차 분석 → 코드베이스 역분석으로 오탐지 제거 → 데이터 레이어 수정 완료 → Training 레이어 수정 진행중

## 개요

- **감사 범위**: 설계 문서 1건 (AGENT.md, ~1050행), 코드 영역 6개 (Settings/Schema, Models, Data, Training, CLI/Factory/Serving, Tests)
- **소스 파일**: `mdp/` 하위 70개 Python 파일 + 23개 테스트 파일 + 카탈로그 YAML

### 핵심 철학 요약

MDP는 **YAML 설정 기반 딥러닝 파이프라인 CLI 도구**로, 세 가지 설계 원칙 위에 세워져 있다:

1. **Two-File System**: Recipe(실험 정의)와 Config(인프라 설정)를 분리하여 동일 실험을 다른 환경에서 재현
2. **`_component_` 패턴**: 모든 플러그인을 동일한 `_component_` 키로 등록. `type:` 키는 존재하지 않음
3. **에이전트 친화적 설계**: `--format json` 출력, Discovery Flow, `???` 마커

### 발견 요약 (검증 후)

| 카테고리 | 전체 | 해결/close | 잔여 |
|----------|:---:|:---:|:---:|
| 문서 의도 우선 (코드 수정 필요) | 24 | 24 | 0 |
| 현재 구현 우선 (문서 업데이트 필요) | 14 | 14 | 0 |
| 논의 필요 | 15 | 14 | 1 |
| /coding 위반 | 22 | 22 | 0 |
| 문서 내부 모순 | 3 | 3 | 0 |
| ~~오탐지 제거~~ | ~~-4~~ | — | — |
| **합계** | **78** | **77** | **1** |

---

## 문서 내부 모순

AGENT.md 내에서 발견된 자기모순 3건 — **모두 해결됨**:

### ~~1. augmentation 예시에서 Dataset alias 오용~~ → ✅ 해결 (`7eaa0bb`)

~~Example 1 (ViT LoRA)에서 `data.augmentation._component_: ImageFolder`를 사용.~~ 전 예시를 `steps: [{type:, params:}]` 형식으로 교정. Dataset alias 테이블도 삭제.

### ~~2. text_classification 카탈로그에 CausalLM 모델~~ → ✅ 해결 (`ae1cabd`)

~~Llama-3-8B, Qwen2.5-7B가 text_classification에 등재.~~ AGENT.md 테이블에서 제거 + 카탈로그 YAML 3개에서 `text_classification` 및 `text_generation` 중복 제거.

### ~~3. WebDataset 참조 불일치~~ → ✅ 해결 (`7eaa0bb`)

~~Data Constraints에서 `WebDataset → path 필수`로 언급.~~ Data Constraints 전면 교체 시 삭제됨.

---

## 핵심 발견

### 문서 의도 우선 — 코드를 문서에 맞춰야 하는 곳

#### [Critical] 런타임 버그 — 코드 직접 검증 완료

**1. ~~`cli/train.py:89` — `create_job()` 필수 인자 누락~~** → ✅ **해결 (`8dd3ee3`)** train.py에서 JobManager 사용을 제거 (학습 결과 추적과 무관한 데드 코드였음).

**2. ~~`cli/inference.py:127` — `compute_baseline()` 파라미터명 불일치~~** → ✅ **해결 (`8dd3ee3`)**

```python
current = compute_baseline(model=model, dataloader=test_loader)
```

`compute_baseline(train_dataloader, model, config, *, max_batches)` — `dataloader`는 존재하지 않는 파라미터. 런타임 `TypeError` 확정.

**3. ~~`data/dataloader.py:90-111` — `drop_last` 이중 전달~~** ✅ 확인 → ✅ **해결 (`d44509b`)**

~~`loader_kwargs = dict(loader_config)` 에서 `drop_last`를 pop하지 않은 채 `_make_loader`의 명시적 `drop_last` 파라미터와 `**loader_kwargs` 모두로 `DataLoader`에 전달.~~ `dl_kwargs.pop("drop_last", True)`로 분리하여 이중 전달 제거.

**4. ~~`models/adapters/qlora.py:81-93` — `modules_to_save`가 `from_pretrained`에 불필요하게 전달~~** → ✅ **해결 (`dc7b6a2`)** from_pretrained 전에 pop

```python
model = model_cls.from_pretrained(..., **kwargs)  # modules_to_save가 kwargs에 포함
modules_to_save = kwargs.pop("modules_to_save", None)  # pop은 정상 작동
```

> **1차 분석 오류 수정**: sub-agent는 "pop이 from_pretrained 이후라 실패한다"고 보고했으나, **Python의 `**kwargs` 언팩은 원본 dict를 수정하지 않으므로 pop은 정상 작동한다**. 실제 문제는 `modules_to_save`가 `from_pretrained`에 불필요하게 전달되는 것으로, HuggingFace `from_pretrained`가 미지원 kwarg를 `__init__`에 전달하여 경고 또는 무시할 수 있다. **크래시 아닌 설계 결함**.

**5. ~~`training/trainer.py:308` — `_log_to_mlflow`가 MLflow context 밖에서 호출~~** → ✅ **해결**

`_log_to_mlflow`를 `_log_mlflow_params`(run 시작 시)와 `_log_mlflow_summary`(finally 블록, run 종료 전)로 분리. 둘 다 `with mlflow_ctx:` 블록 안에서 실행. 추가로 pretrained, dataset_source, batch_size, lr, adapter, strategy 등 실험 재현 params와 epoch_train_loss, learning_rate 에폭 로깅, stopped_reason tag 추가.

#### [Critical] 스키마-문서 구조적 불일치

**6. ~~`DataSpec`이 `_component_` 패턴을 채택하지 않음~~** → ✅ **설계 결정 확정 (`d44509b`)**

DataSpec의 flat 구조(`source` + `fields` + `format`)가 올바른 설계임을 확인. `_component_` 패턴은 dataset에 불필요 — `datasets.load_dataset()`로 통합 로딩하고 source 종류를 자동 감지하는 방식으로 구현 완료. **AGENT.md의 `data.dataset._component_` 기술이 업데이트 필요** (문서 쪽 수정 대상으로 이동).

**7. ~~CLI JSON 출력 계약 전면 불이행~~** → ✅ **해결 (`8dd3ee3`)**

`schemas.py`에 `TrainResult`, `InferenceResult`, `EstimateResult` 등이 정의되어 있으나 **어떤 CLI 핸들러에서도 import/사용하지 않는다**:
- `train.py:103-107`: `build_result(command="train", job_id=..., nproc=...)` — 비표준 필드
- `inference.py:89-93`: `build_result(command="inference", result_path=..., monitoring=...)` — 비표준 필드
- `estimate.py:41`: `build_result(command="estimate", **result)` — flat dict 언팩

**8. ~~JSON 모드에서 stdout 오염~~** → ✅ **해결 (`8dd3ee3`)**

설계 문서: "JSON mode → logs는 stderr, JSON만 stdout". 실제로 `typer.echo()`(기본 stdout)로 텍스트 로그가 출력:
- `train.py:69-70, 86, 93, 96, 100`
- `inference.py:33-34, 57, 76, 83, 131-135`
- `serve.py:31, 50-51, 61, 83, 94-96`

**9. ~~에러 타입 비표준~~** → ✅ **해결 (`8dd3ee3`)** ValidationError/RuntimeError로 표준화

문서: `ValidationError`, `RuntimeError`, `TimeoutError`. 코드: `settings_error`, `training_error`, `inference_error`.

**10. ~~`list` 커맨드 `--format json` 미지원~~** → ✅ **해결 (`8dd3ee3`)** models/tasks/callbacks/strategies 전부 JSON 지원

`is_json_mode()` 분기 전혀 없음. Rich Table만 출력. `ListModelsResult` 등 스키마는 준비되어 있으나 연결 안 됨.

#### [High] Adapter 검증 누락

**11. ~~QLoRA `quantization.bits` 필수 검증 없음~~** → ✅ **해결 (`dc7b6a2`)** error로 검증

**12. ~~QLoRA `torch_dtype` 제약 검증 없음~~** → ✅ **해결 (`dc7b6a2`)** bfloat16/float16 필수 error

**13. ~~`adapter.r` required 제약 미구현~~** → ✅ **해결 (`dc7b6a2`)** lora/qlora 모두 error

**14. `prefix_tuning` 미구현** (`adapters/__init__.py:42-43`) ✅ 확인

문서에 `adapter.method: prefix_tuning → r 필수`로 명시. 코드에서 lora/qlora 외 모든 method는 `ValueError`.

#### [Medium] 검증/계약 누락

**15. ~~warmup_steps + warmup_ratio 동시 지정 시 무음~~** → ✅ **해결**

둘 다 >0이면 `ValueError` 발생하도록 수정.

**16. ~~`val_check_interval` float 처리 불완전~~** → ✅ **해결**

`val_check_unit` 필드 추가 (`"epoch"` | `"step"`). 타입 추론 대신 명시적 단위 지정. 소수 에폭(0.5→에폭당 2회), 정수 에폭(2→매 2에폭), step 기반(500 step) 모두 지원. 기존 fixture에 `val_check_unit: step` 추가.

**17. fp8 precision 미지원** (`trainer.py:79`) ✅ 확인

`else` 분기가 fp32와 fp8 모두 처리. fp8은 아무 AMP 설정 없이 fp32 폴백.

**18. `compat_validator.py:78` — 죽은 task 문자열** ✅ 확인

```python
task not in ("text_generation", "causal_lm", "seq2seq")
```

`"causal_lm"`은 공식 task 목록에 없는 유령 문자열.

#### [Medium] 카탈로그 YAML 오류

**19. ~~text_classification `default_head`에 `vocab_size`~~** → ✅ **해결** bert-base/roberta-base에서 `vocab_size` 제거

**20. ~~segformer `default_head`에 잘못된 필드명~~** → ✅ **해결** `hidden_dim`→`in_channels`, `dropout` 제거

**21. ~~카탈로그 `supported_tasks` 중복~~** → ✅ **해결** feature_extraction 3개 + text_generation 7개 중복 제거

**22. ~~Florence-2 `default_head.task` 불일치~~** → ✅ **해결** `vision_language`는 task가 아님 — `text_generation`으로 변경. `vision_language`를 코드베이스 전체에서 제거 (TASK_HEAD_COMPAT, compat_validator, AGENT.md, fixture, 주석)

#### [Medium] 테스트 fixture 스펙 위반

**23. augmentation에서 `type:` 키 사용** (vit-lora-cifar10.yaml, yolo-detection-custom.yaml, clip-finetune-custom.yaml)

AGENT.md "Common Pitfalls #9" 위반.

**24. 비표준 head import 경로** (gpt2-finetune-text.yaml)

`mdp.heads.CausalLMHead` → 공식: `mdp.models.heads.causal_lm.CausalLMHead`

---

### 오탐지 — 1차 분석에서 제거된 항목

#### ❌ `qlora.py:93` "pop이 from_pretrained 이후라 실패"

**원인**: Python `**kwargs` 언팩은 dict를 복사하여 전달하므로 원본 dict는 수정되지 않음. `kwargs.pop("modules_to_save")` 은 언팩 후에도 정상 작동. 실제 문제는 불필요한 kwarg가 `from_pretrained`에 전달되는 것으로 **심각도 재분류** (P0→P3).

#### ❌ `trainer.py:295` "baseline_info UnboundLocalError 가능성"

**원인**: `baseline_info` 할당은 `try` 블록 마지막에 위치. 이전에 예외가 발생하면 `finally` 실행 후 예외가 전파되어 `with` 블록을 벗어나므로, `baseline_info`를 참조하는 line 317은 **도달 불가**. UnboundLocalError는 발생할 수 없음.

#### ❌ `pretrained.py:68` "rstrip이 잘못된 결과 생성"

**원인**: sub-agent가 `rstrip(":/")` 동작을 오해. `rstrip`은 **문자 집합** `{:, /}`에서 오른쪽 문자를 제거하는 것이지 부분 문자열을 제거하는 게 아님. `"local://"` → `"local"` 정상 동작 확인. `removesuffix("://")`가 의도를 더 명확히 표현하지만 기능 버그는 아님.

#### ❌ `_select_collator`에 token_classification 분기 없음 → "명확한 버그"

**원인**: `LABEL_ALIGN` strategy는 `else` 분기에서 `DataCollatorWithPadding`으로 처리됨. NER 등에서 `DataCollatorForTokenClassification`이 더 적합할 수 있으나, `DataCollatorWithPadding`도 동작하며 명확한 크래시 버그가 아님. **논의 필요**로 재분류.

---

### 현재 구현 우선 — 문서를 코드에 맞춰야 하는 곳

**1. ~~`vision_language` task 확장~~** → ✅ **해결 (`e032d42`)**: vision_language 제거. multimodal은 text_generation/feature_extraction + fields 조합으로 표현. AGENT.md에 명시.

**2. `for_estimation`/`for_inference`에서 validation 생략** (`settings/factory.py:36-55`) — 합리적 설계. AGENT.md에 미기재.

**3. `configure_optimizers()` 모델 오버라이드** (`trainer.py:110-113`) — HuggingFace/Lightning 호환. AGENT.md에 미기재.

**4. ~~`_compute_loss` forward 계약~~** → ✅ **해결**: AGENT.md "Training Internals" 섹션에 forward 계약 명시.

**5. ~~Resume checkpoint 우선순위~~** → ✅ **해결**: AGENT.md "Training Internals" 섹션에 resume 우선순위 명시.

**6. ~~MPS device 지원~~** → ✅ **해결**: AGENT.md "Training Internals" 섹션에 MPS 폴백 명시.

**7. FSDP mixed_precision 기본 활성화** (`strategies/fsdp.py`) — AGENT.md에 미기재.

**8. ~~`LABEL_ALIGN` strategy~~** → ✅ **해결**: AGENT.md "data.fields 역할명과 전처리 매핑" 테이블에 이미 포함.

**9. random_split fallback** (`dataloader.py:154-167`) — val split 없는 데이터셋에 자동 80/20 분할. AGENT.md에 미기재.

**10. ~~`chat_template` 지원~~** → ✅ **해결**: AGENT.md "data.tokenizer 형식"에 이미 포함.

**11. ~~`torch.compile` 모드 값~~** → ✅ **해결**: AGENT.md "Training Internals" 섹션에 compile 모드 명시.

**12. ~~`local://` 보안 경고~~** → ✅ **해결**: AGENT.md "Training Internals" 섹션에 보안 경고 명시.

**13. JSD 사용** (`monitoring/baseline.py:29-44`) — 스키마 필드명 `kl_divergence`이나 실제 구현은 Jensen-Shannon Divergence. 필드명 수정 필요.

**14. Inference/Monitoring forward 계약** — `model(**batch)` 패턴이 문서에 없음.

---

### 논의 필요

**1. ~~`DataSpec` flat vs `_component_` 패턴~~** → ✅ **해결 (`d44509b`)**: flat 구조 + `datasets.load_dataset()` 통합 로딩으로 확정. `_component_`는 dataset에 불필요.

**2. ~~`validate_task_fields` 미호출~~** → ✅ **해결 (`d44509b`)**: `BusinessValidator._check_task_fields`에서 호출하도록 연결.

**3. ~~`TASK_PRESETS` vs `TASK_HEAD_COMPAT` 이원 관리~~** → ✅ **해결 (`e032d42`)** `vision_language` 제거로 9개 vs 8개(feature_extraction 제외). feature_extraction은 head 불필요이므로 COMPAT에 없는 것이 정상. 역할이 다른 두 dict(fields/config 검증 vs head 호환성)이므로 통합 불필요.

**4. ~~`prefix_tuning` 지원 범위~~** → ✅ **해결 (`a3e4261`)** `prefix_tuning.py` 구현, `r` → `num_virtual_tokens` 매핑, BusinessValidator 검증 추가.

**5. ~~CatalogValidator 반환 타입 불일치~~** → ✅ **해결**: `list[str]` → `ValidationResult` 통일. `factory.py:_validate`도 동일 루프로 통합.

**6. `deepspeed_zero3` alias** — `deepspeed`와 동일 클래스 매핑. ZeRO-3 + offload 설정 주입 경로 없음. → **defer** (DeepSpeed 실 사용 시 처리)

**7. ~~`best_metrics` 덮어쓰기~~** → ✅ **해결**: `last_metrics`로 리네임. MLflow prefix도 `best_` → `final_`.

**8. ~~`loader.py` 사용 경로 불분명~~** → ✅ **해결 (`d44509b`)** `load_data`를 `create_dataloaders` 내부에서 호출하도록 통합.

**9. ~~`tokenizer.py:138` target 컬럼 하드코딩~~** → ✅ **해결**: `"summary"` fallback 삭제. target 없으면 `KeyError`.

**10. ~~`max_epochs = 10_000` 매직 넘버~~** → ✅ **해결 (`825e12b`)** `sys.maxsize`로 교체.

**11. ~~`checkpoint.on_batch_end`의 step 기준~~** → ✅ **해결 (`825e12b`)** `kwargs.get("global_step", step)` 사용.

**12. DeepSpeed `gradient_clipping: 1.0` 하드코딩** — `training.gradient_clip_max_norm` 무시 가능성. → **defer** (DeepSpeed 실 사용 시 처리)

**13. ~~`serving: None` 기본값~~** → ❌ **close** 서빙 설정 없이 train/inference 워크플로우에서 None이 맞음. `mdp serve` 시 None이면 에러로 안내.

**14. ~~`head_builtin: true` + `default_head` 공존~~** → ❌ **close** `init.py`에서 `if not head_builtin:`로 분기. 참조용 정보로 카탈로그에 두는 것은 해롭지 않음.

**15. ~~`DualEncoderHead.forward_pair`가 ABC 밖~~** → ❌ **close** contrastive 모델 유일 특수 케이스. ABC 확장은 다른 head에 불필요한 메서드 강제.

---

### /coding 위반

#### 중복 구현

**1. ~~`is_qlora` 판별 로직~~** → ✅ **해결**: `validation/__init__.py`에 `is_qlora()` 헬퍼 추출, business/compat validator에서 호출.

**2. ~~trainable params 로깅~~** → ✅ **해결**: `adapters/__init__.py`에 `log_trainable_params()` 추출, lora/qlora에서 호출.

**3. ~~`_apply_vision_transform` ≡ `_apply_multimodal_transform`~~** → ✅ **해결**: `_apply_multimodal_transform` 삭제, `_apply_vision_transform`으로 통일.

**4. ~~`CausalLMHead` ≡ `Seq2SeqLMHead`~~** → ❌ **close**: 의도적 분리 유지. task별 head 구분 + 향후 seq2seq에 cross-attention 등 확장 가능.

**5. ~~`_make_settings` 헬퍼 8중 복사~~** → ✅ **해결**: `tests/e2e/conftest.py`에 `make_test_settings()` 생성, 8개 파일에서 위임.

**6. ~~`test_phase1_schema_validation.py`~~** → ✅ **해결**: 삭제. pytest 미호환 standalone 스크립트, `test_settings.py`가 동일 검증 커버.

**7. ~~`TestCollatorSelection` ≡ `TestDeriveLabelStrategy`~~** → ❌ **close**: 2개 테스트뿐이고 `derive_label_strategy` 결과 검증이라 실해 없음.

#### 패턴 불일치

**8. ~~logging f-string vs %s 혼용~~** → ❌ **close**: 동작 무관. adapters는 f-string, pretrained.py는 %s. 통일하면 좋지만 기능·성능 영향 없음.

**9. ~~`init.py:149` optimizer full path vs callback alias~~** → ✅ **해결**: `"torch.optim.AdamW"` → `"AdamW"` alias로 통일.

#### 데드 코드 / 미사용

**10. ~~`schemas.py` 전체~~** → ✅ **해결 (`8dd3ee3`)** 데드 필드 17→7 모델 축소. 전 CLI 핸들러에서 스키마 사용.

**11. ~~`ValidationResult.has_errors`~~** → ✅ **해결 (`8dd3ee3`)** 삭제. 테스트도 `len(errors) > 0`으로 수정.

**12. ~~`aliases.yaml` scheduler/loss/evaluator 빈 dict~~** → ✅ **해결**: CosineAnnealingLR, StepLR, LinearLR, CrossEntropyLoss, MSELoss, BCEWithLogitsLoss, Accuracy, F1Score alias 추가.

#### 설계 문제

**13. ~~`check_training_duration` validator가 schema에 위치~~** → ❌ **close**: Pydantic model_validator로 잡는 게 더 이른 시점에 에러를 내므로 실용적. schema docstring이 엄격하지만 "epochs/max_steps 중 하나 필수"는 구조적 제약에 가깝다.

**14. ~~`catalog/__init__.py` 빈 파일~~** → ❌ **close**: `list_cmd.py`에 `_load_models`, `_load_catalog_by_task` 함수가 이미 존재. 별도 catalog API로 분리하는 이득이 현재 규모 대비 적음.

#### 잠재 버그

**15. ~~`dataloader.py:148-152` — bare `except Exception`~~** → ✅ **해결 (`d44509b`)**: `except (ValueError, FileNotFoundError, KeyError)`로 구체화.

**16. ~~`tokenizer.py:135` — shallow `.copy()`~~** → ✅ **해결**: `.copy()` → `[row[:] for row in ...]` deep copy. seq2seq `"summary"` 하드코딩도 제거 (target 없으면 `KeyError`).

**17. ~~NaN loss 허용 테스트~~** → ✅ **해결**: all-masked labels는 CE(mean)가 NaN 반환하는 게 정상. 크래시 미발생만 검증으로 변경.

**18. ~~MLflow experiment_id 하드코딩~~** → ✅ **해결**: `"1"` → `get_experiment_by_name("test-quality")` 이름 기반 조회.

#### 테스트 커버리지 공백

**19-22.** `prefix_tuning` e2e, `scheduler`/`warmup` 검증, `serve`/`estimate` 커맨드, `generation.*` 파라미터, `dataloader` 기본값, `monitoring.drift` 통합, `vision_language` task head — 모두 미테스트.

---

## 권장 액션

### P0: 런타임 버그 수정 — ✅ 전부 해결

1. ~~`cli/train.py:89` create_job~~ → ✅ 해결 (`8dd3ee3`) JobManager 제거
2. ~~`cli/inference.py:127` compute_baseline~~ → ✅ 해결 (`8dd3ee3`) positional args 수정
3. ~~`dataloader.py:90` drop_last~~ → ✅ 해결 (`d44509b`)

### P1: 설계 계약 복원 — 대부분 해결

4. ~~CLI JSON 출력 연결~~ → ✅ 해결 (`8dd3ee3`)
5. ~~JSON 모드 stdout 격리~~ → ✅ 해결 (`8dd3ee3`)
6. ~~에러 타입 표준화~~ → ✅ 해결 (`8dd3ee3`)
7. ~~`list` 커맨드 JSON 지원~~ → ✅ 해결 (`8dd3ee3`)
8. ~~`trainer.py:308` `_log_to_mlflow`~~ → ✅ **해결**: `_log_mlflow_params` + `_log_mlflow_summary`로 분리, `with mlflow_ctx:` 블록 안으로 이동

### P2: 검증 로직 보강

9. ~~**QLoRA 검증**~~ → ✅ **해결 (`dc7b6a2`)**
10. ~~**`adapter.r` required**~~ → ✅ **해결 (`dc7b6a2`)**
11. ~~**warmup 상호배제**~~ → ✅ **해결**: 둘 다 >0이면 `ValueError`
12. **fp8** — 전용 AMP 또는 NotImplementedError

### P3: 카탈로그/Alias/코드 정리

13. **카탈로그 YAML 수정** — `vocab_size` 제거, `hidden_dim`→`in_channels`, `supported_tasks` 중복 제거
14. **`aliases.yaml`** — scheduler/loss alias 추가 또는 빈 카테고리 제거
15. ~~**`qlora.py:81`** — `modules_to_save` pop 순서~~ → ✅ **해결 (`dc7b6a2`)**
16. ~~**테스트 fixture**~~ → ✅ **해결**: `type:` 키는 augmentation DSL로 정상 (오탐지). head/callback import path를 alias로 통일. 미존재 콜백(`ThroughputMonitor`, `GradientNormLogger`) 제거.

### P4: 중복 제거 / 코드 품질

17. ~~**중복 제거**~~ → ✅ **대부분 해결**: `is_qlora` 헬퍼, `log_trainable_params` 헬퍼, `_apply_multimodal_transform` 삭제, `_make_settings` conftest 통합, init.py alias 통일, tokenizer shallow copy/하드코딩 수정, NaN test/MLflow test 수정. 잔여: `CausalLMHead`/`Seq2SeqLMHead` (의도적 유지)
18. **dead code 제거** — `test_phase1_schema_validation.py` (standalone 스크립트, 낮은 우선순위)
19. ~~**`task_taxonomy.py`** — `TASK_PRESETS`와 `TASK_HEAD_COMPAT` 통합~~ → 부분 해결: `validate_task_fields`가 BusinessValidator에 연결됨. `TASK_HEAD_COMPAT` 통합은 잔여

### P5: 문서 업데이트

20. ~~**AGENT.md** — data 스키마, 예시, alias, constraints, 모순 수정~~ → ✅ **대부분 해결 (`7eaa0bb`, `ae1cabd`)**. 잔여: `vision_language` TASK_PRESETS 등재, `configure_optimizers()`, resume checkpoint 우선순위, forward 계약, `torch.compile` 모드 값, MPS 지원, `local://` 보안 경고
21. ~~**AGENT.md 내부 모순 수정**~~ → ✅ **해결 (`7eaa0bb`, `ae1cabd`)**

---

## 해결 이력

| 커밋 | 해결 항목 |
|------|----------|
| `d44509b` | P0#3 drop_last 이중 전달, #6 DataSpec/_component_ 설계 확정, Factory↔create_dataloaders 연결, fields 컬럼 매핑, validate_task_fields 연결, load_data 파이프라인 통합, bare except 구체화 |
| `7eaa0bb` | AGENT.md 전면 정합: data 스키마 교체, 4개 예시 교정, augmentation/tokenizer 형식 문서화, Dataset alias 삭제, Data Constraints 교체, head._target_attr 추가, prefix_tuning 미구현 명시, init.py 기본 템플릿 수정 |
| `ae1cabd` | text_classification 카탈로그에서 CausalLM 모델 제거 (AGENT.md + catalog YAML 3개), supported_tasks 중복 제거 |
| `8dd3ee3` | CLI JSON 계약 복원: schemas.py 데드 필드 정리, 전 핸들러 스키마 연결, stdout 격리, 에러 타입 표준화, list JSON 지원, create_job/compute_baseline 버그 수정, has_errors 제거 |
| `dc7b6a2` | QLoRA 검증 강화: adapter.r/quantization.bits/torch_dtype 필수 error 추가, modules_to_save pop 순서 수정, fixture 교정 |
| (미커밋) | JobManager 제거 (MLflow로 대체), MLflow 로깅 전면 개선 (#5 context 버그 수정, params/summary 분리, 실험 재현 params 추가), val_check_interval 재설계 (#16 `val_check_unit` 필드 추가, 소수 에폭/step 기반 mid-epoch 검증 지원), warmup 상호배제 검증 (#15) |
| `263d905` | 카탈로그 default_head 수정: bert/roberta vocab_size 제거, segformer hidden_dim→in_channels |
| `e032d42` | vision_language를 task에서 제거, 9-task taxonomy 통일, supported_tasks 중복 제거 10개 파일, compat_validator causal_lm 유령 문자열 제거 |
| `a3e4261` | prefix_tuning 구현: adapter 라우팅, r→num_virtual_tokens 매핑, BusinessValidator 검증 |
| (미커밋) | /coding 위반 정리: is_qlora·log_trainable_params 헬퍼 추출, _apply_multimodal_transform 삭제, _make_settings conftest 통합, init.py alias 통일, tokenizer shallow copy·하드코딩 수정, NaN test·MLflow test 수정, best_metrics→last_metrics 리네임, fixture head/callback alias 통일·dead callback 제거, CatalogValidator 반환타입 ValidationResult 통일 |
